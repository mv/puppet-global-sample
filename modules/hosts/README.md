# Puppet: /etc/hosts


In case I need to fix /etc/hosts....


### Usage

##### hosts

    # install
    include hosts

    # reset: put just localhost in it
    include hosts::reset

    # test: myself!
    include hosts::myself

### itens created

    /etc/hosts


# vim:ft=markdown

